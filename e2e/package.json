{
  "name": "@reaatech/webhook-relay-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@reaatech/webhook-relay-core": "workspace:*",
    "@reaatech/webhook-relay-storage": "workspace:*",
    "@reaatech/webhook-relay-webhooks": "workspace:*",
    "@reaatech/webhook-relay-tools": "workspace:*",
    "@reaatech/webhook-relay-mcp": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
